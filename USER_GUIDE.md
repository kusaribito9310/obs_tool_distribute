# OBSTool 完全操作ガイド

対象: OBSTool 1.0.19以降 / Windows 10・11（64bit）

OBSToolは、Discordのボイスチャンネル参加者・発言状態・テキストチャットを、OBSのブラウザソースへ透過表示するローカルアプリです。処理は利用者のPC上で動き、常時稼働サーバーやNode.jsのインストールは必要ありません。

## 目次

1. [必要なもの](#必要なもの)
2. [初回セットアップ](#初回セットアップ)
3. [Discord連携設定](#discord連携設定)
4. [デザイン設定](#デザイン設定)
5. [プレビューと配置](#プレビューと配置)
6. [OBSへの追加](#obsへの追加)
7. [通常の使い方](#通常の使い方)
8. [権限・認証・保存の仕組み](#権限認証保存の仕組み)
9. [最新版への更新](#最新版への更新)
10. [トラブルシューティング](#トラブルシューティング)
11. [配布者・管理者向け](#配布者管理者向け)

## 必要なもの

- Windows 10またはWindows 11（64bit）
- Discordデスクトップ版
- OBS Studio
- インターネット接続
- 配布された `OBSTool.exe`
- 対象Discordサーバーへ導入済みの専用Bot

ブラウザー版Discordだけでは本人認証と発言検知を利用できません。OBSToolを使う本人のアカウントでDiscordデスクトップ版へログインしてください。

初めて使う場合は次から最新版を取得できます。

- [最新版 OBSTool.exe](https://github.com/kusaribito9310/obs_tool_distribute/releases/latest/download/OBSTool.exe)
- [配布ページ](https://github.com/kusaribito9310/obs_tool_distribute/releases/latest)

## 初回セットアップ

### 1. Discordを起動する

Discordデスクトップ版を起動し、実際に配信で使用するアカウントへログインします。

### 2. OBSToolを起動する

`OBSTool.exe`を任意のフォルダーへ保存して起動します。インストール操作はありません。

Windows SmartScreenが表示された場合は、配布元とファイル名を確認し、「詳細情報」から「実行」を選択します。現在のexeはコード署名されていないため、GitHubから取得した場合でも警告が出ることがあります。

### 3. Discord連携を承認する

初回だけDiscordの承認画面が表示されます。内容とアプリ名を確認して承認してください。承認後は暗号化した認証情報をWindowsユーザーごとに保存し、通常の再起動では承認画面を出しません。

操作画面上部に `利用者: 表示名 — 本人の閲覧権限で表示します。` と表示されれば認証完了です。

### 4. サーバーIDを取得する

1. Discordの「ユーザー設定」→「詳細設定」を開きます。
2. 「開発者モード」を有効にします。
3. 対象サーバーのアイコンを右クリックします。
4. 「サーバーIDをコピー」を選びます。

### 5. 表示対象を保存する

1. OBSToolの「Guild（サーバー）ID」へコピーしたIDを貼り付けます。
2. 「取得」を押します。
3. カテゴリを選びます。
4. 表示するVCとテキストチャンネルへチェックを入れます。
5. 「設定を保存して反映」を押します。

## Discord連携設定

### 利用者と表示名

画面上部には、現在Discordで本人確認できている利用者が表示されます。「表示名を強制更新」を押すと、表示対象VCの参加者と表示済みチャット投稿者の名前・画像をすぐに再取得します。

通常はDiscordの変更イベントに加え、約15秒ごとに自動確認します。サーバー内ニックネーム、Discord表示名、ユーザー名の順で名前を採用します。キャラクター名を設定している場合は、その名前を優先します。

### カテゴリ・VC・テキストチャンネル

本人とBotの両方が閲覧できるチャンネルだけが一覧に表示されます。鍵付きカテゴリでも本人に権限があれば選択でき、本人に権限がなければBotが管理者でも表示されません。

- **監視ボイスチャンネル**: 参加者と発言状態をOBSへ表示します。
- **監視テキストチャンネル**: 新着メッセージと起動時の直近履歴を表示します。
- **VCの並び順 / Discordと同じ**: Discord上の位置順です。
- **VCの並び順 / 任意に並べる**: 一覧の上下ボタンで順番を決めます。
- **VCの並び順 / 50音順**: 日本語名を基準に並べます。

テキスト履歴には、本人とBotの両方に「チャンネルを見る」と「メッセージ履歴を読む」が必要です。新着本文の取得にはBot側のMessage Content Intentも必要です。

### キャラクター差し替え

現在表示対象になっているVC参加者ごとに設定できます。

- **表示キャラ名**: Discord名の代わりに表示する固定名です。
- **立ち絵/画像URL**: Discordアイコンの代わりに使用する画像のURLです。

URLはOBSを実行するPCから直接取得できるHTTPS画像を推奨します。URLが失効するサービス、ログインが必要なURL、HTMLページのURLは表示できないことがあります。

## デザイン設定

### 表示方法

- **テキストチャットを1つの枠にまとめる**: 選択した全テキストチャンネルを時系列で1つの枠へ表示します。投稿名の前にチャンネル名が付きます。
- **VCをチャンネルごとに分割表示**: VCごとに独立した枠として配置できます。
- **テキストをチャンネルごとに分割表示**: テキストチャンネルごとに独立した枠として配置できます。
- **VC参加者を画像メインで表示**: アイコン中心の表示から立ち絵向け表示へ切り替えます。
- **各チャンネルのタイトルを表示**: オフにすると、VC名とテキストチャンネル名の見出しを非表示にします。
- **VC参加者の名前を表示**: オフにすると参加者名を非表示にします。画像メイン表示では名前を画像の下へ表示し、長い名前は下方向へ折り返します。

「1つの枠にまとめる」と「テキストをチャンネルごとに分割表示」を両方有効にした場合は、1つの枠にまとめる設定を優先します。

画像メイン表示の並べ方は次の3種類です。

| 設定 | 動作 |
| --- | --- |
| グリッド | 枠内へ格子状に並べます。 |
| 横一列 | 参加者を横方向へ並べます。 |
| キャラ別の自由配置 | プレビューで参加者を個別に移動できます。クリックした参加者が手前になります。右端の青いハンドルで横幅、右下のピンク色ハンドルで縦横比を保った拡大・縮小ができます。 |

### 発言中の演出

「跳ねる」「発光・ネオン」「拡大」は複数同時に選択できます。

- **拡縮時の画像倍率**: 「拡大」の最大倍率です。1.00～2.00倍で設定します。
- **発光色**: 発言中の発光色です。
- **発光対象**: アイコンと名前を個別に選べます。
- **非発言時はグレー、発言時は原色**: 発言者を見つけやすくします。
- **グレーの強さ**: 0～100%で調整します。

### 色

VC枠・チャット枠の背景色、透明度、文字色を設定できます。画像メイン表示では「画像の名前背景」で画像下の名前ラベルの色と透明度を、「画像の名前サイズ」で文字サイズ（8～72px）を変更できます。文字色には「VC文字色」を使います。名前を隠す場合は「VC参加者の名前を表示」をオフにします。

透明度Aは0で完全透明、1で不透明です。VC枠・チャット枠・画像の名前背景の各スライダーと数値欄は連動し、0.01刻みで入力できます。変更はプレビューへ即時反映されます。再起動後も残すには「設定を保存して反映」を押してください。OBS上の背景は透過されます。

## プレビューと配置

右側のプレビューは1920×1080を基準にしています。

OBSのシーンを見ながら位置を決めるには、OBSでシーンのスクリーンショットを保存し、プレビュー上部の「OBSシーン画像を読み込む」でその画像を選択してください。ブラウザソース自体をOBSで非表示にしてから撮影すると、二重表示を避けられます。画像は1920×1080のプレビュー全体に合わせて表示され、その上で今までどおり枠やキャラクターをドラッグできます。「背景を消す」で黒い背景に戻ります。読み込んだ画像はこの編集画面だけで使用し、配信画面や設定ファイルには保存しません。シーンの内容が変わったら画像を選び直してください。

| 操作 | 結果 |
| --- | --- |
| 枠の中央付近をドラッグ | 枠を移動します。 |
| 枠の辺・四隅をドラッグ | 幅または高さを変更します。 |
| Shiftを押しながら辺・四隅をドラッグ | 縦横比を保って内容全体を拡大・縮小します。 |
| 自由配置のキャラクターをドラッグ | キャラクターを個別に移動します。 |
| 自由配置のキャラクター右端をドラッグ | キャラクター画像の横幅を変更します。名前の折り返しと選択範囲も幅に追従します。 |
| 自由配置のキャラクター右下をドラッグ | キャラクター画像を縦横比を保ったまま拡大・縮小します。 |
| 「位置リセット」 | VC・チャットの位置とキャラクター個別配置を初期位置へ戻します。 |

数値欄ではX座標、Y座標、横幅、高さ、拡大率を直接入力できます。VCでは立ち絵の高さと画像の重なりも設定できます。高さを0にすると内容に合わせた自動高さになります。

左パネルと右パネルの間にある縦線を左右へドラッグすると、設定欄とプレビュー欄の表示幅を調整できます。この画面上の幅調整はOBSの配置値には影響しません。

分割表示を有効にすると、数値欄の上にチャンネル切り替えボタンが表示されます。調整したいチャンネルを選んでから位置・サイズ・拡大率を変更してください。

プレビューへの変更は即時表示されますが、アプリ再起動後も残すには「設定を保存して反映」を押してください。

## OBSへの追加

1. OBSToolを起動したままにします。
2. OBSで対象シーンを開きます。
3. ソースの「＋」から「ブラウザ」を追加します。
4. URLへ `http://localhost:8080/overlay` を入力します。
5. 幅を `1920`、高さを `1080` にします。
6. 必要に応じて「現在のページのキャッシュを更新」を実行します。

OBS用URLへ `?mode=editor` は付けません。これはOBSTool内部の編集プレビュー専用です。

ローカル配信は利用者PC内で動きます。OBSToolを終了するとOBSの表示も停止します。

## 通常の使い方

配信時は次の順で起動すると安定します。

1. Discordデスクトップ版
2. OBSTool
3. OBS Studio

「一時停止」を押すと通常の状態配信を止めます。「再開する」を押すと現在状態と履歴を再送します。権限剥奪、アカウント変更、Discord切断が確認された場合は、一時停止中でも安全のため表示を解除します。

チャットは時間経過だけでは消えません。名前更新、VC状態更新、配置変更、テーマ変更、ローカル配信接続の一時切断でも表示済みの本文を保持します。チャンネルを切り替えると対象外の履歴は画面から隠れ、同じチャンネルを再選択するとアプリ内に保持した履歴を再表示します。権限解除・アカウント変更による表示リセット、OBSブラウザソースの再読み込み、アプリ再起動では履歴が消えます。

## 権限・認証・保存の仕組み

### 閲覧権限

OBSToolはDiscordデスクトップ版で本人を確認し、Bot経由で本人のロールとチャンネル個別権限を調べます。本人とBotの両方が閲覧できる対象だけを扱います。権限は約30秒ごとにも再確認します。

一時的な通信失敗だけでは、確認済みの表示と購読を解除しません。実際の権限変更、アクセス拒否、アカウント変更、Discord切断、Bot切断では表示を解除します。Bot再接続後は保存済みのカテゴリ・VC・テキスト選択を復元します。

### 保存場所

設定とログは次に保存されます。

| 内容 | 場所 |
| --- | --- |
| 表示・チャンネル設定 | `%APPDATA%\obs-discord-overlay\user_config.json` |
| 暗号化されたDiscord認証 | `%APPDATA%\obs-discord-overlay\rpc-auth.bin` |
| 現在のログ | `%APPDATA%\obs-discord-overlay\logs\app.log` |
| 退避された前回ログ | `%APPDATA%\obs-discord-overlay\logs\app.previous.log` |
| UTF-8ではなかった旧ログ | `%APPDATA%\obs-discord-overlay\logs\app.invalid-encoding.log` |

`Win + R`を押し、上記の`%APPDATA%`から始まるパスを貼り付けると開けます。`rpc-auth.bin`は同じWindowsユーザーでのみ復号できます。

## 最新版への更新

### 更新パッチを使う

1. [最新版の更新パッチ](https://github.com/kusaribito9310/obs_tool_distribute/releases/latest/download/OBSTool-update.exe)を取得します。
2. OBSToolを終了します。
3. `OBSTool-update.exe`を起動します。
4. 「参照」で現在使用しているOBSToolのexeを選びます。
5. 「更新する」を押します。

更新パッチはどの旧バージョンからでも、そのパッチに内蔵された最新版へ直接更新できます。将来の版をオンライン取得するものではないため、毎回最新の更新パッチを取得してください。

更新前のexeは同じフォルダーへ `元の名前.exe.旧版.識別子.bak` として保存されます。`user_config.json`と`rpc-auth.bin`は変更されません。

### 通常exeへ置き換える

更新パッチが使えない場合は、最新版`OBSTool.exe`を別名または別フォルダーへ保存して起動できます。同じWindowsユーザーならAppDataの設定と認証を引き継ぎます。

## トラブルシューティング

### 「WindowsによってPCが保護されました」と表示される

現在のexeはコード署名されていないため、SmartScreenが表示されることがあります。GitHubの正式な配布ページから取得したことを確認し、「詳細情報」→「実行」を選択します。自己署名証明書だけでは、配布先PCで証明書を信頼登録しない限り警告は解消しません。

### アプリが起動しない、8080番ポート使用中と表示される

- 既に起動しているOBSToolがないか確認します。二重起動時は既存画面が前面に出ます。
- 開発版を`npm start`で起動している場合は、そのターミナルで`Ctrl + C`を押します。
- タスクマネージャーで残っているOBSToolまたはElectronを終了します。
- 別アプリが8080番を使っている場合は、そのアプリを終了します。

OBSToolはポートを使用している別プロセスを自動終了しません。

### 「Discordデスクトップ版で認証してください」が続く

1. ブラウザー版ではなくDiscordデスクトップ版を起動します。
2. 使用するアカウントでログインしているか確認します。
3. DiscordとOBSToolを終了し、Discordから先に起動します。
4. Discordの「ユーザー設定」→「連携済みアプリケーション」で承認状態を確認します。
5. 再承認する場合はOBSToolを終了し、`%APPDATA%\obs-discord-overlay\rpc-auth.bin`を削除して起動します。

`user_config.json`を消す必要はありません。認証ファイルを別PCや別Windowsユーザーへコピーしても利用できません。

### 「本人の閲覧権限を確認中…」から進まない

- OBSTool 1.0.17以降へ更新します。
- Botが対象サーバーへ参加し、オンラインになっているか確認します。
- 入力したサーバーIDが正しいか確認します。
- 本人が対象サーバーへ参加しているか確認します。
- Discordまたはネットワークが一時切断していないか確認します。
- 「取得」を一度押し直します。

1.0.17以降では、通常の表示名確認でこの表示へ定期的に戻ったり、カテゴリ・VC・テキスト選択が消えたりしません。

### カテゴリまたはチャンネルが一覧に出ない

- 本人に「チャンネルを見る」権限があるか確認します。
- Botにも「チャンネルを見る」権限があるか確認します。
- カテゴリと子チャンネルで権限が同期されていない場合は、子チャンネル側も確認します。
- サーバーIDとDiscordアカウントが正しいか確認します。
- ロール変更後は最大30秒ほど待つか、再度「取得」を押します。

本人が見られない鍵付きチャンネルを一覧へ出すことはできません。

### チャンネル選択が消える、元に戻る

- OBSTool 1.0.17以降へ更新します。
- 最後に「設定を保存して反映」を押します。
- 実際に権限が外れたチャンネルは安全のため選択から除外されます。
- 別のWindowsユーザーやPCでは設定保存先が異なります。

1.0.17以降は、表示名確認、同じ権限状態の通知、初回権限取得、Bot再接続、保存前のカテゴリ再取得で選択を維持します。

### OBSに何も表示されない

- OBSToolを起動したままにします。
- OBSのURLが `http://localhost:8080/overlay` か確認します。
- 幅1920、高さ1080を設定します。
- VCまたはテキストチャンネルを選択して保存したか確認します。
- OBSブラウザソースの「現在のページのキャッシュを更新」を実行します。
- ブラウザーで同じURLを開き、ローカル表示自体が動くか確認します。

### VC参加者は表示されるが発言演出が動かない

- Discordデスクトップ版が起動しているか確認します。
- 対象VCを選択して保存します。
- 「跳ねる」「発光・ネオン」「拡大」のいずれかを有効にします。
- Discord RPCが切断されていないかログを確認します。

### テキストチャットが表示されない

- 対象テキストチャンネルを選択して保存します。
- 本人とBotの「チャンネルを見る」を確認します。
- 過去ログには本人とBotの「メッセージ履歴を読む」も必要です。
- 新着本文にはBotのMessage Content Intentが必要です。
- Botの投稿は表示対象外です。

### 表示名が変わらない

「表示名を強制更新」を押します。キャラクター差し替えの「表示キャラ名」が設定されている場合は、その固定名が優先されます。自動更新は通常15秒以内ですが、通信状況や対象人数で遅れることがあります。

### 画像・立ち絵が表示されない

- 画像そのものを直接開けるHTTPS URLか確認します。
- URLの期限切れ、アクセス制限、ログイン要求を確認します。
- WebページのURLではなく、画像ファイルを返すURLを指定します。
- OBSブラウザソースのキャッシュを更新します。

### チャットが消えた

時間経過だけでは消えません。権限解除、選択解除、Discord切断、OBSブラウザソースの再読み込み、アプリ再起動では消えます。アプリ再起動をまたぐチャット履歴の完全保存は行いません。起動時には選択チャンネルの直近履歴を最大5件ずつ取得します。

### 更新パッチが失敗する

- 更新対象のOBSToolを終了します。
- 対象exeのファイル名に`OBSTool`を含めます。
- 書き込み可能なフォルダーへexeとパッチを置きます。
- 新版とバックアップを置ける空き容量を確保します。
- パッチ自体が最新か確認します。
- 解決しない場合は最新版の通常exeを取得します。

### ログが文字化けする

`npm start`は1.0.16以降でコンソールをUTF-8へ切り替えます。配布exe利用時や外部ツールで文字化けする場合は、UTF-8 BOM付きの次のファイルをメモ帳で開いてください。

```text
%APPDATA%\obs-discord-overlay\logs\app.log
```

すでに文字化けした文字列は元に戻せません。最新版で再起動して新しいログを確認します。
UTF-8として読み込めない古い`app.log`は起動時に`app.invalid-encoding.log`へ退避し、新しい`app.log`をUTF-8 BOM付きで作り直します。

### 設定を完全に初期化したい

1. OBSToolを終了します。
2. `%APPDATA%\obs-discord-overlay`をバックアップします。
3. 表示設定だけを初期化する場合は`user_config.json`を削除します。
4. Discord承認だけをやり直す場合は`rpc-auth.bin`を削除します。
5. 両方を初期化する場合は2ファイルとも削除します。

### 問題報告に必要な情報

- OBSToolのバージョン
- 問題が起きた時刻
- 操作手順
- 画面に表示されたメッセージ
- `%APPDATA%\obs-discord-overlay\logs\app.log`

ログや設定を共有する前に内容を確認してください。`user_config.json`には設定や認証情報が含まれる場合があるため、そのまま公開しないでください。

## 配布者・管理者向け

### Discord側の準備

- DiscordアプリケーションとBotを作成します。
- Botを対象サーバーへ一度招待します。
- Botへ必要なチャンネルの閲覧・履歴取得権限を付与します。
- Message Content Intentを有効にします。
- RPC/OAuthで`rpc`と`identify`を利用できるようにします。
- 利用対象アカウントが必要に応じてアプリを利用できる状態にします。

Botに管理者権限は必須ではありません。OBSToolのDiscordスラッシュコマンドはありません。

### 認証情報と配布

ビルド時の`.env`には次を設定します。

```dotenv
DISCORD_BOT_TOKEN=
DISCORD_RPC_CLIENT_ID=
DISCORD_RPC_CLIENT_SECRET=
DISCORD_RPC_REDIRECT_URI=http://127.0.0.1
```

現在の配布方式は`.env`をexeへ同梱します。画面で非表示でも、exeからBot TokenやClient Secretを抽出できるため、信頼できる相手だけへ配布してください。

### 開発起動・ビルド

```bat
npm ci --include=dev
npm start
```

配布物を作る場合:

```bat
build.cmd --no-pause
```

生成物:

- 最新版: `dist/latest/OBSTool.exe`
- 全旧版対応パッチ: `dist/latest/OBSTool-update.exe`
- バージョン別保管: `releases/バージョン/SHA256/`

GitHub Releaseへ公開する場合:

```bat
publish-github.cmd
```

詳細は[配布ガイド](DISTRIBUTION.md)、[パッチ更新ガイド](PATCHING.md)、[開発ガイド](DEVELOPMENT.md)を参照してください。
